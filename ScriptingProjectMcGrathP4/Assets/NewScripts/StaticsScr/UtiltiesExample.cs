using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UtiltiesExample : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int x = Utilties.Add(5, 6);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
